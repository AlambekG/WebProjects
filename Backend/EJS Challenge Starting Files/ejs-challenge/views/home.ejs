<%- include("partials/header") -%>

<h1>Home</h1>
<p> <%= content %></p>
<% console.log(postArr) %>
<% postArr.forEach(function( element) { %>
 <h1> <%= element.title%> </h1>
 <p> <%= element.content.substring(0, Math.min(element.content.length, 100)) + " ..." %> 
    <a href="posts/<%= element.title %>">Read More</a>
</p>
<%}) %>

<%- include("partials/footer") -%>